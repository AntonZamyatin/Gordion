{
  "name": "deckgl-ribbons-spike",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Phase 1 throwaway spike: prove deck.gl PathLayer perf + picking at ~200k ribbons.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@deck.gl/core": "^9.1.0",
    "@deck.gl/layers": "^9.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}